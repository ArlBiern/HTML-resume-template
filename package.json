{
  "name": "mysite",
  "version": "1.0.0",
  "description": "About me page",
  "main": "index.js",
  "scripts": {
    "test": "npm run test:html && npm run build:autoprefixer && npm run test:css",
    "test:html": "glob-run nu-html-checker index.html",
    "build:autoprefixer": "autoprefixer-cli css/style.css",
    "test:css": "css-validator css/style.css"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer-cli": "^1.0.0",
    "css-validator": "^0.9.0",
    "glob-run": "^0.1.7",
    "nu-html-checker": "^0.1.0"
  }
}
